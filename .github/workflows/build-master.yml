name: Build

on:
  push:
    branches: [ "master" ]

env:
  REPO_PATH: ${{ vars.REPO_PATH }}

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build
      run: echo ${{ vars.REPO_PATH }}
